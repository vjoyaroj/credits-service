package nttdata.bootcamp.credits_service;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Bootstrap for the Credits microservice.
 */
@SpringBootApplication
public class CreditsServiceApplication {

	/**
	 * @param args standard Spring Boot arguments
	 */
	public static void main(String[] args) {
		SpringApplication.run(CreditsServiceApplication.class, args);
	}

}
